# Simulateur Amadeus (monorepo)

## Installation

```bash
npm install
```

## Lancer le frontend

```bash
npm run dev:web
```

## Tests

```bash
npm run test
```

## Build du frontend

```bash
npm run build:web
```

## Structure

- `apps/web` : frontend React (Vite)
- `packages/core` : moteur de commandes (core)
- `packages/data` : store de données (DataStore + seeds)
